package com.gayathri.projects.entity;

/*In java enum is a special type of class with a fixed set of instances */
/*we are creating enum instead of class as we only have constants and the constants are limited*/
public enum ConfidentialityLevel
{

    Confidential,
    Non_Confidential,
    Restricted
}

